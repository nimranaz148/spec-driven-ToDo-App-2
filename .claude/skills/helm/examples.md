# Helm Examples

## Example 1: Create a Blueprint Chart
"Create a new Helm chart named 'todo-app' following the standard blueprint."

## Example 2: Override Values
"Install the 'todo-app' chart with replicas set to 3 and image.tag set to 'v2.0'."

## Example 3: Template Debugging
"Render the templates for 'todo-app' to verify the generated manifests."
