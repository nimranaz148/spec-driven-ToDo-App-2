apiVersion: v1
kind: Service
metadata:
  name: mcp-service
  namespace: todo-app
  labels:
    app: todo-chatbot
    component: mcp
spec:
  selector:
    app: todo-chatbot
    component: mcp
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
